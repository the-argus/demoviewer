const std = @import("std");
const types = @import("types.zig");

/// a function to read BSPs which currently only prints its findings to log
pub fn read_bsp(relative_path: []const u8) !void {
    _ = relative_path;
}
